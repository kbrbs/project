# Copy this file to .env (or set env vars in your hosting environment)
# Do NOT commit your real credentials into version control.

# Choose console backend for local development (prints emails to console):
DJANGO_EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# To enable SMTP delivery, set the following and restart the app/process manager:
# Example (Mailtrap/testing provider):
# DJANGO_EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# SMTP_HOST=smtp.mailtrap.io
# SMTP_PORT=587
# SMTP_USER=your_mailtrap_user
# SMTP_PASSWORD=your_mailtrap_password
# SMTP_USE_TLS=True
# SMTP_USE_SSL=False

# DEFAULT_FROM_EMAIL is the address that appears in 'From' headers when sending
DEFAULT_FROM_EMAIL=webmaster@localhost
